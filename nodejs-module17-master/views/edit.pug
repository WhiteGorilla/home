extends layout

block content
	h1 Все курсы Академии
	form(method="post")
		fieldset.form-group
			label(for="name") Название:
			input.form-control(name="name", type="text", placeholder="Введите название курса", value=course.name)
		button.btn.btn-primary(type="submit") Создать курс
		a.btn.btn-default(href="/api/courses") Отмена